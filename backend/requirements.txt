fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3
pydantic==2.10.4
pydantic-settings==2.7.0
python-jose[cryptography]==3.3.0
argon2-cffi==23.1.0
python-multipart==0.0.20
email-validator==2.2.0
razorpay==1.4.2
setuptools==75.6.0
httpx==0.28.1
tenacity==9.0.0
python-dotenv==1.0.1
gunicorn==23.0.0
slowapi==0.1.10
# Imported directly by app/core/cache.py, and used by limits' Redis storage
# backend for shared rate-limit counters. It arrives transitively via
# slowapi/limits today, but both of those are runtime imports in our own
# code, so it is declared rather than relied on as a transitive dependency.
redis==8.1.0

# dev / test
pytest==8.3.4
pytest-asyncio==0.25.0
pytest-cov==6.0.0
faker==33.1.0
# Required by tests/test_cache.py, which errors on import without it.
fakeredis==2.37.0
