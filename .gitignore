# macOS
.DS_Store

# Local Claude Code state (machine-specific permissions, not shared config)
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Node
node_modules/

# Secrets — every real .env stays local. Only *.example files are committed.
.env
.env.local
*.env.local
